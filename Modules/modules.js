const userCard = {
  name: 'Sabrine Ben Salah',
  address: 'Tunisia'
};

export const address = userCard.address;

export function getUserName() {
  return userCard.name;
}

// export default userCard;